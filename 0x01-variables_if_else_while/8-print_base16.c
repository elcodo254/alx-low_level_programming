#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

	int numy;
	int anumy;

	for (numy = 48; numy <= 57; numy++)
		putchar(numy);
	for (anumy = 97; anumy <= 102; anumy++)
		putchar(anumy);
	putchar('\n');
	return (0);
}
