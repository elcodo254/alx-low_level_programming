#include "main.h"
/**
 * _strcmp - compares two strings
 *@s1: first string
 *@s2: second string
 *Return: 0 if contains same string
 */
int _strcmp(char *s1, char *s2)
{
	int index;

	for (index = 0; s1[index]; index++)
	{
	}
	for (index = 0; s2[index]; index++)
	{
	}
	if (s2[index] == s1[index])
		return (0);
	else
		return (s2[index] - si[index]);
}
