#include "main.h"

/**
 * print_alpha - prints the alphabet in lowercase,followed by a new line.
 * Return: always 0 (Success)
 */

void print_alpha(void)
{
	char letters;

	for (letters = 'a'; letters <= 'z'; a++)
	{
		_putchar(letters);
	}

	putchar('\n');

	return (0);
}
