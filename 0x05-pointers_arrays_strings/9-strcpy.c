#include "main.h"

/**
 * *_strcpy - copy string
 * @src: pointer
 * @dest: buffer
 * Return: pointer to dest
 */
char *_strcpy(char *dest, char *src)
{
	int i, length;

	for (length = 0; src[length] != '\0'; lenth++)
	{
	}

	for (i = 0; i <= length; i++)
	{
		dest[i] = src[i];
	}
	return (dest);
}
